# Minimal dproxy configuration

server:
  listen_addr: "0.0.0.0"
  ports:
    udp: 53
    tls: 853
    https: 443

cache:
  enabled: true

routing:
  default:
    upstreams:
      - doh://cloudflare-dns.com/dns-query
    strategy: failover

